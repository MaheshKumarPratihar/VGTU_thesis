@startuml TextClustering_State_EndToEnd
!theme cerulean-outline
title Text Clustering Lifecycle - End-to-End Process

[*] --> Submitted : "User uploads dataset"

Submitted --> DataValidation : "Start processing"

DataValidation --> DataRejected : "Invalid format"
DataValidation --> Preprocessing : "Valid dataset"

DataRejected --> [*]

Preprocessing --> BalanceCheck : "Text cleaned"

BalanceCheck --> GAIAugmentation : "Imbalanced"
BalanceCheck --> EmbeddingReady : "Balanced"

GAIAugmentation --> AugmentationValidation : "Synthetic samples generated"

AugmentationValidation --> GAIAugmentation : "Quality < 0.85"
AugmentationValidation --> EmbeddingReady : "Quality >= 0.85"

EmbeddingReady --> EmbeddingGeneration : "Start embedding generation"

EmbeddingGeneration --> ParameterOptimization : "Embeddings ready (All models)"

ParameterOptimization --> GAIParameterSuggestion : "Request GAI suggestions"

GAIParameterSuggestion --> Training : "Parameters configured"

Training --> Evaluation : "Clustering complete"

Evaluation --> QualityCheck : "Metrics calculated"

QualityCheck --> GAIAnalysis : "Quality < 0.70"
QualityCheck --> Interpretation : "Quality >= 0.70"

GAIAnalysis --> Training : "Re-train with new params"

Interpretation --> GAILabeling : "Generate cluster labels"

GAILabeling --> InterpretationValidation : "Labels created"

InterpretationValidation --> GAILabeling : "Confidence < 0.80"
InterpretationValidation --> Visualization : "Confidence >= 0.80"

Visualization --> ResultsReady : "Plots & reports generated"

ResultsReady --> [*] : "Available for export"

note right of GAIAugmentation
  GAI Enhancement #1:
  Generates synthetic samples
  to balance dataset
end note

note right of GAIParameterSuggestion
  GAI Enhancement #2:
  Suggests optimal
  SOM parameters
end note

note right of GAILabeling
  GAI Enhancement #3:
  Automatically labels
  and interprets clusters
end note

note right of ResultsReady
  PRIMARY OUTPUT:
  - Cluster assignments
  - Quality metrics
  - GAI interpretations
  - Visualizations
end note

@enduml
